set(CURRENT_DIR mainview)

set(SOURCE_FILES
        ${SOURCE_FILES}
        ${CURRENT_DIR}/UIMainView.cpp
        ${CURRENT_DIR}/UIMainView_Base.cpp
        ${CURRENT_DIR}/UIMainView_Category.cpp
        )


